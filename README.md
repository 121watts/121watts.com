# resume-site

Clean, static resume site built with Astro and Tailwind CSS.

## Edit content (single source of truth)
All resume content lives in:
- `src/content/resume.json`

## Run locally
```bash
npm install
npm run dev
```

## Deploy (GitHub Pages)
Deploy via GitHub Actions (workflow in `.github/workflows/`).
